export enum Team {
    Radiant = "radiant",
    Dire = "dire",
}

export enum GameCloseness {
    CLOSE = "close game",
    STOMP_LOSS = "stomp loss",
    STOMP_WIN = "stomp win",
    COMEBACK = "comeback",
    THROW = "throw"
}